export default function Home() {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">Hello 👋</p>
        <h1>kpclick12</h1>
        <p className="lede">
          This is my corner of the internet — a placeholder for now, but the
          deploy pipeline is live. Every push to <code>main</code> ships.
        </p>
        <nav className="links" aria-label="Elsewhere">
          <a href="https://github.com/kpclick12">GitHub</a>
        </nav>
      </section>
    </main>
  );
}
